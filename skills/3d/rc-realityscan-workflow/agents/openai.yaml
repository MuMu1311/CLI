interface:
  display_name: "RC RealityScan Workflow"
  short_description: "RC setup, crop, training, export, recovery"
  default_prompt: "Use $rc-realityscan-workflow to create, run, crop, train, monitor, export, or recover a RealityScan/RC production workflow safely."
